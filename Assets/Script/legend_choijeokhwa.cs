using UnityEngine;

public class legend_choijeokhwa : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Application.targetFrameRate = 60; // 혹은 144
        QualitySettings.vSyncCount = 0;   // 수직동기화 해제 (필요시)
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
