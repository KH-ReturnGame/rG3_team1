# 아트 작업 가이드 (프로크리에이트 → 레드 후드)

> 아트 담당자에게 그대로 전달하는 문서. 이 규칙만 지키면 그림이 게임에 **크기 조정 없이, 순간이동 버그 없이** 바로 들어갑니다.
> 규칙이 생긴 이유는 맨 아래 "실제로 났던 사고" 참고.

## ⚡ 30초 요약 (이것만 지켜도 90% 해결)

1. **캔버스는 항상 1024×1024** — 템플릿 파일로 시작
2. **캐릭터 키는 항상 같게** — 서 있는 키 = 초록선(600px). 매번 이 기준
3. **제자리에서 그리기** — 이동/점프해도 그림 속 캐릭터는 캔버스의 같은 자리에. 움직임은 게임이 시킴
4. **지상 동작 = 발을 빨간 바닥선에** / **공중 동작 = 몸 중심을 십자에**
5. **내보내기 = 투명 배경 PNG, 프레임별로** (배경·가이드 레이어 끄고!)
6. 보낼 때 **메모 필수**: 뭐 하는 동작인지, 몇 프레임인지, 1초에 몇 프레임인지, 반복(루프)인지

---

## 1. 시작 설정

- 프로젝트 폴더의 **`ArtTemplates/템플릿_지상용_1024.png`** 와 **`템플릿_공중용_1024.png`** 를 아이패드로 보내세요(에어드랍/카톡).
- 프로크리에이트에서 **새 캔버스 1024×1024px** → 템플릿 이미지를 **가이드 레이어로 삽입**(동작 → 추가 → 파일/사진 삽입) → 불투명도 30% 정도로 낮추고 **잠금**.
- 그 위에 새 레이어를 만들어 그립니다. 애니메이션은 **애니메이션 어시스트** 켜고 1프레임 = 1레이어(또는 그룹).

## 2. 그리기 규칙

### 크기 — "키 600px"이 세계의 기준
- **주인공이 서 있는 키 = 초록 머리끝선(600px).** 모든 동작(대기/달리기/공격/점프)에서 몸 크기가 같아야 합니다.
- **몬스터/NPC는 주인공 대비 상대 크기**로: "주인공 절반쯤" → 300px, "주인공 1.5배 거인" → 900px. 새 캐릭터를 만들 땐 메모에 "주인공 대비 몇 배" 한 줄 적어주세요.
- 무기 휘두르기·망토 등이 캔버스를 벗어나면? → 몸 크기는 그대로 두고 **캔버스만 키우세요**(예: 1536×1024). 절대 몸을 줄여서 맞추지 마세요.

### 위치 — "제자리에서" (제일 중요!)
- **이동·점프·돌진의 '움직임'은 게임 코드가 시킵니다.** 그림에서 캐릭터를 캔버스 안에서 이동시키면, 게임 움직임과 겹쳐 **순간이동/이단 점프처럼 보이는 버그**가 됩니다.
- 구르기(2단 점프) 같은 것도: 캐릭터가 **제자리에서 회전만** 하게 그려주세요. 위로 뜨는 건 게임이 처리.
- 프레임마다 캐릭터가 미세하게 어긋나는 것도 떨림으로 보입니다 — 이전 프레임을 반투명(어니언 스킨)으로 켜고 같은 자리에 그리기.

### 지상 vs 공중 (템플릿 2종을 나눠 쓰는 이유)
| 동작 종류 | 템플릿 | 규칙 |
|---|---|---|
| 서기·걷기·달리기·지상 공격 등 **땅에 있는 동작** | 지상용 | **발이 빨간 바닥선에 닿게** |
| 점프·낙하·공중 공격·비행 몬스터 등 **떠 있는 동작** | 공중용 | **몸통 중심을 빨간 십자에** |

## 3. 내보내기 (프로크리에이트)

1. **배경 레이어 끄기** + **가이드(템플릿) 레이어 끄기** ← 잊으면 게임에 바닥선까지 나옵니다
2. 애니메이션: **동작 → 공유 → PNG 파일** (프레임이 각각의 PNG로 저장됨)
   - ❌ GIF/움직이는PNG/MP4로 보내지 않기 — 변환하면서 화질·투명도가 상합니다
   - 한 장짜리(초상화·아이콘 등)는 그냥 **PNG** (❌ JPEG — 투명도가 없어짐)
3. **자르지 말고 캔버스 통째로** — 여백이 있어도 괜찮습니다. 여백 정리는 개발 쪽에서 자동으로 합니다. 오히려 프레임마다 다르게 자르면 위치가 틀어집니다.

## 4. 보내는 방법

폴더 하나에 담아서:
```
슬라임_대기/
  ├ frame_01.png, frame_02.png, ... (순서대로 번호)
  └ 메모.txt  ←  "슬라임 평상시 통통거리는 동작. 6프레임, 1초에 10프레임, 계속 반복"
```
메모에 꼭 들어갈 것: **① 어떤 캐릭터의 어떤 동작 ② 프레임 수 ③ 1초당 프레임 수(빠르기) ④ 반복(루프)인지 한 번만인지** (+ 새 캐릭터면 주인공 대비 크기)

## 5. 실제로 났던 사고 (규칙이 생긴 이유)

- **크기 사고**: 2단 점프·공중공격이 2048 캔버스에 제각각 크기로 그려져서, 게임에서 캐릭터가 **5배 크기**로 나옴 → 일일이 수치를 재서 보정해야 했음. → "키 600px 고정" 규칙이면 무보정.
- **순간이동 사고**: 구르기 프레임들이 캔버스 안에서 위로 이동하며 그려져서, 게임 점프와 겹쳐 **캐릭터가 위로 순간이동한 것처럼** 보임. 공중 동작과 지상 동작의 기준점이 달랐던 것도 겹침 → "제자리 + 지상/공중 템플릿" 규칙이면 예방.
- **배경 사고**: 도움말 영상/GIF에 배경색이 깔려 나와 배경 제거 작업이 필요했음 → "투명 배경 PNG" 규칙.

## 6. 자주 묻는 질문

- **Q. 기존 캔버스(2048 등)로 작업하던 파일은?** → 상관없음. 단 그 안에서 "키 600px 기준의 2배 = 1200px"처럼 **비율을 지켜주고 메모에 캔버스 크기**를 적어주면 됨. 새 작업은 1024 템플릿 권장.
- **Q. 프레임 수는 몇 개가 좋아?** → 대기 4~6, 걷기 6~8, 공격 4~7, 이런 느낌. 1초당 8~12프레임이 이 게임 스타일에 맞음.
- **Q. 좌우 방향은?** → **오른쪽을 보게** 그려주세요. 왼쪽은 게임이 자동으로 뒤집습니다. (좌우 비대칭 디자인—한쪽에만 칼집 등—은 뒤집힐 때 반대가 되니 미리 알려주기)
- **Q. 색은?** → 자유. 단 피격 시 게임이 흰색/노랑 등을 살짝 덧씌우는 연출이 있어서, 순백(255,255,255) 위주의 캐릭터는 피격 표시가 안 보일 수 있음.
